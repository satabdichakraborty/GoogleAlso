package com.algorithms.may.thirtythone.test;

import java.util.Arrays;

public class ValidateSubsequenceTest {

	class ProgramTest {
		/*
		 * @Test public void TestCase1() { var array = Arrays.asList(5, 1, 22, 25, 6,
		 * -1, 8, 10); var sequence = Arrays.asList(1, 6, -1, 10);
		 * Utils.assertEquals(true, Program.isValidSubsequence(array, sequence)); }
		 */}

}
